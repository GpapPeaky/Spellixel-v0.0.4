#include "ui.h"

void present_ui(int room_number){

    /* Takes the room data and all data that it is required to show, And displays it on the screen ( either a number or a texture, unsure of the numbers -> SDL_ttf has to be included ) */

    return;
}